{% set image = {
    'src': 'http://via.placeholder.com/320x180',
    'width': 320,
    'height': 160,
    'alt': 'placeholder image',
    'fluid': true
} %}

{% set sources = [
  {
      'srcset': 'http://via.placeholder.com/640x360',
      'media': '(min-width: 600px)',
      'classes': ['img-fluid']
  }
] %}

{% set picture = {
    'picture_sources': sources,
    'picture_img': image,
} %}


{% include '@molecules/media/picture.twig' with picture %}
