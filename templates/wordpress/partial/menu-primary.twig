

{% import '@macros/menu-test.twig' as menus %}

{% set menu_id = 'menu-' ~ menu.id  %}
{% set menu_classes = ['navbar-nav', 'mr-auto'] %}

{{ menus.print_menu( menu.items, menu_id, menu_classes, 'ul', 'li' ) }}
